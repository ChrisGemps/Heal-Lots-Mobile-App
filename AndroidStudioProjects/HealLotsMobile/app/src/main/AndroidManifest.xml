<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera & Storage for Profile Pictures -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealLots"
        tools:targetApi="31">

        <!-- MainActivity - Splash Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HealLots">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication Activities -->
        <activity
            android:name=".features.auth.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

        <activity
            android:name=".features.auth.register.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

        <!-- Main Activities -->
        <activity
            android:name=".features.appointments.dashboard.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

        <activity
            android:name=".features.appointments.book.BookAppointmentActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

        <activity
            android:name=".features.appointments.list.MyAppointmentsActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

        <activity
            android:name=".features.profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

        <activity
            android:name=".features.admin.dashboard.AdminDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.HealLots" />

    </application>

</manifest>
